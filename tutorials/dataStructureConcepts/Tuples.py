# empty tuple
tupleA = tuple()
tupleA = ()


# Immutable Objects i.e. size or value of any item cannot change after declaration.
# Declared using brackets i.e. ()
# tuples are immutable and does not alllow assignment or extending the tuple.

vowels = ('a', 'i', 'e', 'o', 'u')

# tuples should have atleast 2 objects in it otherwise its not a tuple its the same as the single object in it.

tuple1 = ('kid')        # string not a tuple.
tuple2 = (2)            # integer not a tuple.
tuple3 = ('Kid', )      # single object tuple.
